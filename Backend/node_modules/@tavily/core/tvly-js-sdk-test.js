require("dotenv").config();
// tvly-js-sdk-test.js (CommonJS)
const { tavily } = require('./dist/index.js');

async function run() {
  const tvly = tavily({ apiKey: process.env.TAVILY_API_KEY }); // <-- put your real key
  const res = await tvly.extract("https://ca.linkedin.com/in/rajesh-palli-59598556",{extract_depth: "advanced", timeout: 90});
  console.log(res);
}
run();
